<UserControl
    x:Class="Fido2.Manager.Views.CredentialsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:Fido2.Manager.ViewModels"
    xmlns:ctap="using:Fido2.Core.Ctap2">

    <Grid RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Spacing="8">
            <Button Content="列出凭据" Command="{x:Bind ViewModel.ListCommand}" />
            <Button Content="删除选中"
                    Command="{x:Bind ViewModel.DeleteCommand}"
                    CommandParameter="{x:Bind CredentialsList.SelectedItem, Mode=OneWay}"
                    IsEnabled="{x:Bind ViewModel.HasCredentials, Mode=OneWay}" />
            <TextBlock Text="{x:Bind ViewModel.MetadataText, Mode=OneWay}"
                       VerticalAlignment="Center" Margin="12,0,0,0"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        </StackPanel>

        <TextBlock Grid.Row="1" Text="{x:Bind ViewModel.EmptyText, Mode=OneWay}"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                   TextWrapping="Wrap" />

        <ListView Grid.Row="2" x:Name="CredentialsList"
                  ItemsSource="{x:Bind ViewModel.Credentials, Mode=OneWay}"
                  SelectionMode="Single">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="ctap:ResidentCredential">
                    <StackPanel Spacing="2" Padding="0,4">
                        <TextBlock Text="{x:Bind RpId}" FontWeight="SemiBold" />
                        <TextBlock Text="{x:Bind BestUserName}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        <TextBlock Text="{x:Bind CredentialIdHex}" FontSize="11"
                                   Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                   IsTextSelectionEnabled="True" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
