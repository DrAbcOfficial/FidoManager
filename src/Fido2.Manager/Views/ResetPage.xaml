<UserControl
    x:Class="Fido2.Manager.Views.ResetPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <StackPanel Spacing="16" MaxWidth="720" HorizontalAlignment="Left">
        <TextBlock x:Name="WarningMain" TextWrapping="Wrap" />
        <TextBlock x:Name="WarningCritical" TextWrapping="Wrap"
                   FontWeight="SemiBold" Foreground="#C42B1C" />
        <TextBlock x:Name="WindowHint" TextWrapping="Wrap"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        <Button x:Name="ResetButton" Command="{x:Bind ViewModel.ResetCommand}"
                Background="#C42B1C" Foreground="White" />
    </StackPanel>
</UserControl>
