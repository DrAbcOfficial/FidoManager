namespace Fido2.Core;

/// <summary>Display strings the library embeds in its own record properties but whose
/// wording belongs to the host UI. The host overwrites them once at startup; defaults are
/// English so library-only consumers get something reasonable.</summary>
public static class CoreStrings
{
    public static string Unnamed = "(unnamed)";
}
